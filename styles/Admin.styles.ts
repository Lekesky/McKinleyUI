import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    container: {
        paddingTop: 65,
    },
    title: {
        fontSize: 50,
        fontWeight: "bold",
        marginVertical: 10,
        textAlign: "center",
    },
    subtitle: {
        fontSize: 16,
        color: "#666",
        marginBottom: 20,
    },
    buttonGroup: {
        width: "100%",
        gap: 10,
    },
});

export default styles;